import React from 'react';
import PersonContainer from '../components/People/PeopleContainer';

export default ({ people }) => (
    <div>
        <h1>People</h1>
        <PersonContainer />
    </div>
);
